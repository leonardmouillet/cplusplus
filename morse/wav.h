#include <vector>
#include <string>
#include <iostream>
#include <fstream>
#include <cmath>


void morse_to_sound(const std::string &filename, std::string &morse_code);
std::string sound_to_morse(const std::string &filename);